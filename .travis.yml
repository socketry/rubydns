language: ruby
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y bind9
rvm:
  - 2.2
  - 2.1
  - 2.0.0
  - 1.9.3
  - rbx-2
matrix:
  allow_failures:
    - rvm: rbx-2 
env: COVERAGE=true
